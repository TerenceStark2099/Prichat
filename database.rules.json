{
  "rules": {
    "rooms": {

      ".read": "true",
      ".write": "true",

      "$roomCode": {
        "$messageId": {
          ".validate": "newData.hasChildren(['message', 'timestamp', 'senderID'])"
        }
      }
    },
    ".read": "false",
    ".write": "false"
  }
}